# Daily Log: 2026-08-11

## Sessions

## Memory Maintenance

### Session (16:06)

**Context:** Building and publishing a GitHub Pages site for the IPT Workflow repo, plus associated documentation and branding cleanup.

**Key Exchanges:**
- Q: What does `lmc install` actually do? A: Clones the repo, runs `uv sync`, and installs a launcher to `~/.local/bin/lmc`; `lmc init` is not needed since the repo carries its own config and hooks.
- Q: Why does Jekyll cache cause a bad "Next" link on the last page? A: Liquid `assign`s persist on a reused (cached) template object across pages — fixed by resetting `prevpage`/`nextpage` to `false` before the loop.
- Q: Why were 15 cross-references left as backticked filenames? A: They were inert on the rendered site; converted to real relative links so they work on both GitHub and the Pages site.
- Q: What's the GitHub Free vs. paid plan tradeoff for Pages? A: Free plan requires the repo to be **public** to enable Pages — which would expose the exec summary, open questions, and security posture material publicly. Pro/Team/Enterprise allows Pages from private repos.

**Decisions Made:**
- Renamed all references to *this* repo from "IPT Framework" → "IPT Workflow" in `README.md` and `EXECUTIVE-SUMMARY.md`, deliberately leaving NIST RMF and other external framework mentions untouched.
- Jekyll site uses `jekyll-optional-front-matter`, `jekyll-titles-from-headings`, and `jekyll-relative-links` (GitHub Pages defaults) so docs stay plain Markdown with no front matter required.
- Navigation order and page metadata are centralized in `_data/nav.yml` — single source of truth for both sidebar and prev/next links.
- README note added: if the build ever moves to GitHub Actions, those three plugins must be declared explicitly or titles and cross-links break.

**Lessons Learned:**
- AI image models (SDXL-based) reliably garble multi-word labels in infographics; Flux or Ideogram handle text better. Text-light prompts (labels added in Canva/Figma afterward) are the most reliable path to a usable result.
- The changelog instruction in Contributing pointed at a stub that only exists in `templates/ipt-charter-template.md` — the six `docs/` files don't have changelog sections, so the instruction was misleading.
- Local directory `/Users/rweiss/git/IPT-Framework` and the remote bundle `IPTFramework.bundle` still use the old name; safe to rename with `mv` from a fresh shell since hooks now use `$CLAUDE_PROJECT_DIR`.

**Action Items:**
- Confirm GitHub plan (Free vs. Pro/Team) before enabling Pages — determines whether repo must go public.
- Once Pages is live, send URL/screenshot to verify CSS and table rendering (the RACI table in `docs/03` is the most likely layout problem).
- Optional: add changelog stubs to the six `docs/` files to match the template's pattern.
- Optional: align local directory name and bundle name to `IPT-Workflow`.
- Optional: author an SVG infographic of the five-phase pipeline for the Pages landing page (more reliable than an AI-generated raster for correct gate names and week numbers).
- Optional: do a pass replacing "model" with "workflow" throughout `docs/` if single-term consistency is desired.

